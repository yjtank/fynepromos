<!DOCTYPE html><html lang="pt-BR"><head><meta charset="UTF-8"><meta name="viewport" content="width=device-width, initial-scale=1.0"><title>{{ $offer->title }} — FynePromos</title>@vite(['resources/css/app.css','resources/js/app.js'])</head><body class="min-h-screen bg-slate-950 text-slate-100"><header class="border-b border-slate-800"><div class="mx-auto flex max-w-5xl items-center justify-between px-6 py-5"><a href="{{ route('home') }}" class="text-2xl font-black">Fyne<span class="text-indigo-400">Promos</span></a><a href="{{ route('home') }}" class="text-sm text-slate-300">← Voltar às ofertas</a></div></header><main class="mx-auto max-w-5xl px-6 py-10"><div class="grid overflow-hidden rounded-3xl border border-slate-800 bg-slate-900 md:grid-cols-2">@if($offer->image_url)<div class="flex min-h-96 items-center justify-center bg-white p-8"><img src="{{ $offer->image_url }}" alt="{{ $offer->title }}" class="max-h-96 max-w-full object-contain"></div>@else<div class="flex min-h-96 items-center justify-center bg-slate-800 text-slate-500">Sem imagem</div>@endif<div class="p-8"><p class="text-sm font-semibold uppercase tracking-widest text-indigo-300">{{ $offer->category->name }} · {{ $offer->store->name }}</p><h1 class="mt-3 text-3xl font-black">{{ $offer->title }}</h1><div class="mt-7">@if($offer->old_price)<p class="text-slate-500 line-through">R$ {{ number_format($offer->old_price, 2, ',', '.') }}</p>@endif<p class="text-4xl font-black text-emerald-400">R$ {{ number_format($offer->current_price, 2, ',', '.') }}</p>@if($offer->installment_info)<p class="mt-2 text-slate-400">{{ $offer->installment_info }}</p>@endif</div>@if($offer->coupon)<p class="mt-6 rounded-lg bg-amber-400/10 p-3 text-amber-300">Cupom: <strong>{{ $offer->coupon }}</strong></p>@endif<a href="{{ route('offers.click', $offer) }}" class="mt-8 block rounded-xl bg-indigo-500 px-5 py-4 text-center font-bold text-white hover:bg-indigo-400">Ver oferta na {{ $offer->store->name }} →</a><p class="mt-4 text-center text-xs text-slate-500">Você será redirecionado para a loja.</p></div></div>@if($offer->description)<div class="mt-8 rounded-2xl border border-slate-800 bg-slate-900 p-6"><h2 class="mb-3 text-xl font-bold">Descrição</h2><p class="whitespace-pre-line text-slate-300">{{ $offer->description }}</p></div>@endif</main></body></html>
